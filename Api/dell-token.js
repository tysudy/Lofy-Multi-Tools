module.exports.self = function () {
    const colors = require('colors')
    console.clear()
    console.log("   [!] Não disponível, aguarde até a próxima atualização".yellow)
    process.exit(1)
}